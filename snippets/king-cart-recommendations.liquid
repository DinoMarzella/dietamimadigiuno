<div class="w-full">
  <!-- 
<div class="hidden col-span-2 dev-hidden ">
<section aria-labelledby="cart-upsells" class="w-full max-w-full md:block "
  v-if="cart && cart.items.length > 0 && !isAllUpsellinCart() ">
  <div class="flex flex-col items-start lg:items-start lg:mb-2 lg:py-2 border-b border-[#00000029]" v-cloak>
    <div class="flex items-center mb-2 ">
      <p class="font-bold text-md md:text-2xl">+ Recommended with your prolon purchase </p>
    </div> 
    {% comment %} <div>
      <p class="m-0 mb-2">Continue your longevity journey</p>
    </div> {% endcomment %}
  </div>

  <div class="w-full overflow-scroll">
    <div class="flex justify-start  md:!grid grid-cols-2 gap-4 mt-4 lg:grid-cols-3 lg:mt-0">


<template v-for="item in recommendedUpsells">

  <single-upsell :item="item" :cart="cart" />
</template>
<div class="flex items-center w-full">
  <div class="flex flex-col justify-center items-center bg-[#f8faf9] p-4 px-0 pt-0 pb-6 rounded-lg col-span-1 w-full h-full relative overflow-hidden ">

    {% comment %} <div  class="w-full flex items-center gap-x-1.5 text-center bg-red-100 px-2 py-1 text-xs font-medium text-red-700 mb-3">
    <div class="font-extrabold text-center text-transparent text-md bg-clip-text bg-gradient-to-r from-prolon to-orange-600 ">
    
    </div>
    </div> {% endcomment %} 
    <div class="absolute top-0 right-0 w-12 h-12">
      <div
        class="absolute transform rotate-45 bg-prolon text-center text-white font-semibold py-1 right-[-35px] top-[32px] w-[170px]">
      Most Popular
      </div>
    </div>
    <div class="flex px-2 mt-8">
      <div 
      v-for="(item,index) in recommendedUpsells" class="flex items-center" 
      :data-index="index"
      :data-length = "recommendedUpsells.length"
      >
      <img :src="item.featured_image" class="w-20 h-20 rounded-full" alt="" />
      <div class="px-2" v-if="index<recommendedUpsells.length-1" >
        +
      </div>
      </div>

    </div>

    
    <p class="pt-4 font-bold text-center text-black">Total Price: $125.00</p> 
    <div class="w-40">
    <button class="flex flex-col justify-center h-10 px-8 bg-white text-prolon rounded-3xl !mx-auto">
        <div class="flex items-center justify-center cursor-pointer"><span class="text-xs font-bold whitespace-nowrap">Add both to the cart</span>
        </div>
    </button>
    </div>
  </div>
</div>
    </div>
  </div>
</section>

</div> --> 

 


<section aria-labelledby="cart-upsells" class="w-full max-w-full md:block "
  v-if="cart && cart.items.length > 0 && !isAllUpsellinCart() ">
  <div class="flex flex-col items-start lg:items-start lg:mb-8 lg:p-6 border-b border-[#00000029]" v-cloak>
    <div class="flex items-start mb-2 m:text-2xl ">
      <h2>Frequently bought together</h2>
    </div>
    <div>
      <p class="m-0 mb-2">Continue your longevity journey</p>
    </div>
  </div>  
  
  <div class="w-full overflow-scroll">
  <div class="flex justify-start  md:!grid grid-cols-2 gap-4 mt-4 lg:grid-cols-3 lg:mt-0">
 
    
      <template v-for="item in recommendedCollection">
      
        <single-upsell :item="item" :cart="cart" />
      </template>
  </div>
  </div>
</section> 
</div>