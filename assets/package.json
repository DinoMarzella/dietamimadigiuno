{
  "scripts": {
    "build": "npx tailwindcss -i .\/ck-tailwind.css -o .\/ck-tailwind-build.min.css --minify",
    "staging": "theme watch --env=staging",
    "live": "theme watch --allow-live"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.6"
  },
  "dependencies": {
    "@tailwindcss\/typography": "^0.5.10"
  }
}
